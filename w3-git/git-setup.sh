# Install Git on EC2 Instance
sudo yum install git -y

# First configuration for Git
git config --global user.email "s4023710@rmit.edu.vn"
git config --global user.name "quihuynhlediem"
git config --global init.defaultBranch main